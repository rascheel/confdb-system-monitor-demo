name: cpu-monitor
version: '0.1'
summary: Exposes CPU data via ConfDB
description: |
  A learning experiment using mpstat to feed the Snapd ConfDB.
confinement: strict
base: core24

plugs:
  cpu-stats:
    interface: confdb
    account: <YOUR_ACCOUNT_ID>
    view: cpu-stats/monitor
    role: custodian

hooks:
  load-view-cpu-stats:
    plugs:
      - system-observe
  save-view-cpu-stats:
    plugs:
      - system-observe

parts:
  monitor:
    plugin: dump
    source: .
    stage-packages:
      - sysstat
      - bc
